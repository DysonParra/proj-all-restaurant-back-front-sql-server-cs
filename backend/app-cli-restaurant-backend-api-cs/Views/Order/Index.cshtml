@model IEnumerable<Project.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IntCustomerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntMealId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IntCustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntMealId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntOrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntOrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
