@model IEnumerable<Project.Models.Meal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FltPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntChefId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FltPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntChefId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntMealId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntMealId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntMealId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
